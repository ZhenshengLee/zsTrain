//#include "include/math.h"
#include <iostream>
#include "calculate.h"
float cal(int a, int b);

int main(void)
{
    int a=20;
    int b=10;
    std::cout << cal(a,b) << std::endl;

    // printf("add(%d,%d)=%d\n",a,b,add(a,b));
    // printf("sub(%d,%d)=%d\n",a,b,sub(a,b));
}
