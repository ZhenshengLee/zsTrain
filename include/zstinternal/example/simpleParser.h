#pragma once

#include <string>

using namespace std;

class simpleParser
{
  public:
    simpleParser();
    ~simpleParser();
    // 成员函数
    int parseAndSum(string numbers);

  private:
    // 私有成员

    // 私有方法
};
